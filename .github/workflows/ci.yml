# SPDX-License-Identifier: GPL-2.0-or-later
#
# kairos CI — two-gate pipeline.
#
# Gate 1 (build-and-test): all compilers, Release build, runs on push + PR.
#   - nomos-rt, txlog, edn-cpp, and nomos-topology checked out at current main
#     (main-tracking integration).
# Gate 2 (sanitize):       Clang only, Debug + ASan + UBSan, PR only,
#                           runs only after gate 1 is fully green.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    name: build / ${{ matrix.os }} / ${{ matrix.compiler }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-24.04, compiler: gcc-14,     cc: gcc-14,   cxx: g++-14     }
          - { os: ubuntu-24.04, compiler: clang-18,   cc: clang-18, cxx: clang++-18 }
          - { os: macos-15,     compiler: apple-clang, cc: clang,   cxx: clang++    }

    steps:
      - uses: actions/checkout@v4

      - name: Cache CMake FetchContent deps
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: ${{ runner.os }}-${{ matrix.compiler }}-kairos-cmake-deps-${{ hashFiles('CMakeLists.txt') }}
          restore-keys: ${{ runner.os }}-${{ matrix.compiler }}-kairos-cmake-deps-

      - name: Check out nomos-rt @ main
        uses: actions/checkout@v4
        with:
          repository: nomos-studio/nomos-rt
          path: deps/nomos-rt

      - name: Check out txlog @ main
        uses: actions/checkout@v4
        with:
          repository: nomos-studio/txlog
          path: deps/txlog

      - name: Check out edn-cpp @ main
        uses: actions/checkout@v4
        with:
          repository: nomos-studio/edn-cpp
          path: deps/edn-cpp

      - name: Check out nomos-topology @ main
        uses: actions/checkout@v4
        with:
          repository: nomos-studio/nomos-topology
          path: deps/nomos-topology

      - name: Install system deps (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update -qq && sudo apt-get install -y libasound2-dev libsqlite3-dev

      - name: Configure
        env:
          CC:  ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: |
          cmake -S . -B build --preset ci \
            -DCMAKE_C_COMPILER=${{ matrix.cc }} \
            -DCMAKE_CXX_COMPILER=${{ matrix.cxx }} \
            -DNOMOS_RT_DIR=${{ github.workspace }}/deps/nomos-rt \
            -DTXLOG_CPP_DIR=${{ github.workspace }}/deps/txlog/cpp \
            -DEDN_CPP_DIR=${{ github.workspace }}/deps/edn-cpp \
            -DNOMOS_TOPOLOGY_SRC_DIR=${{ github.workspace }}/deps/nomos-topology

      - name: Build
        run: cmake --build build --parallel

      - name: Test
        run: ctest --test-dir build --output-on-failure

  sanitize:
    name: sanitize / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: build-and-test
    if: github.event_name == 'pull_request'
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-24.04, cc: clang-18, cxx: clang++-18 }
          - { os: macos-15,     cc: clang,    cxx: clang++    }

    steps:
      - uses: actions/checkout@v4

      - name: Cache CMake FetchContent deps (sanitize)
        uses: actions/cache@v4
        with:
          path: build-san/_deps
          key: ${{ runner.os }}-clang-kairos-san-cmake-deps-${{ hashFiles('CMakeLists.txt') }}
          restore-keys: ${{ runner.os }}-clang-kairos-san-cmake-deps-

      - name: Check out nomos-rt @ main
        uses: actions/checkout@v4
        with:
          repository: nomos-studio/nomos-rt
          path: deps/nomos-rt

      - name: Check out txlog @ main
        uses: actions/checkout@v4
        with:
          repository: nomos-studio/txlog
          path: deps/txlog

      - name: Check out edn-cpp @ main
        uses: actions/checkout@v4
        with:
          repository: nomos-studio/edn-cpp
          path: deps/edn-cpp

      - name: Check out nomos-topology @ main
        uses: actions/checkout@v4
        with:
          repository: nomos-studio/nomos-topology
          path: deps/nomos-topology

      - name: Install system deps (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update -qq && sudo apt-get install -y libasound2-dev libsqlite3-dev

      - name: Configure
        env:
          CC:  ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: |
          cmake -S . -B build-san --preset sanitize \
            -DCMAKE_C_COMPILER=${{ matrix.cc }} \
            -DCMAKE_CXX_COMPILER=${{ matrix.cxx }} \
            -DNOMOS_RT_DIR=${{ github.workspace }}/deps/nomos-rt \
            -DTXLOG_CPP_DIR=${{ github.workspace }}/deps/txlog/cpp \
            -DEDN_CPP_DIR=${{ github.workspace }}/deps/edn-cpp \
            -DNOMOS_TOPOLOGY_SRC_DIR=${{ github.workspace }}/deps/nomos-topology

      - name: Build
        run: cmake --build build-san --parallel

      - name: Test
        run: ctest --test-dir build-san --output-on-failure

  format-check:
    name: clang-format
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install clang-format-18
        run: |
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key \
            | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc >/dev/null
          echo "deb http://apt.llvm.org/noble/ llvm-toolchain-noble-18 main" \
            | sudo tee /etc/apt/sources.list.d/llvm-18.list
          sudo apt-get update -qq
          sudo apt-get install -y clang-format-18

      - name: Check formatting
        run: |
          find include src tests -name '*.cpp' -o -name '*.hpp' | \
            xargs clang-format-18 --dry-run --Werror
